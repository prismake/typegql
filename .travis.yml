language: node_js
node_js:
  - 8
  - 10
cache:
  yarn: true
before_install:
  - npm i codecov -g
scripts:
  - yarn test --ci
after_success:
  - codecov
  - npm i typescript -g
deploy:
  provider: npm
  email: capajj@gmail.com
  api_key: $NPM_TOKEN
  on:
    tags: false
